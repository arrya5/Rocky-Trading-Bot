{
  "name": "Swing v2 — Donchian + ADX + Sector RS + ATR stops",
  "description": "Professional swing strategy. Donchian breakout, ADX trend filter, sector RS, ATR-adaptive stops, max 10-day hold.",
  "starting_capital": 500000,
  "rules": {
    "min_score": 60,
    "adv_min_cr": 50,
    "vol_max_pct": 3.5,

    "max_hold_days": 10,
    "circuit_gap_pct": 0.18,
    "vix_max": 25,
    "fii_min_cr": -3500,
    "sector_max_open": 2,

    "size_tier_high": 70000,
    "size_tier_mid":  50000,
    "size_tier_low":  30000
  },
  "costs": {
    "stt_sell_pct": 0.001,
    "brokerage_per_order": 20
  },
  "exit_logic": "ATR-adaptive per-trade (stop=2x ATR, partial=1.5x ATR, trail=2.5x ATR trigger)",
  "gates_skipped": ["3 (catalyst — LLM lookahead)", "8 (earnings — defer)"]
}
