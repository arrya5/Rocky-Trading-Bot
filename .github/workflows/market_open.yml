name: Market Open Execution

on:
  schedule:
    - cron: "50 3 * * 1-5"  # 03:50 UTC = 09:20 IST, Mon–Fri
  workflow_dispatch:

concurrency:
  group: rocky-routine
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  market-open:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: requirements-runner.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-runner.txt

      - name: Run market-open routine
        env:
          GEMINI_API_KEY:     ${{ secrets.GEMINI_API_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID:   ${{ secrets.TELEGRAM_CHAT_ID }}
          UPSTOX_ACCESS_TOKEN: ${{ secrets.UPSTOX_ACCESS_TOKEN }}
          PAPER_TRADING:       "true"
          PYTHONUTF8:          "1"
        run: |
          python scripts/runners/market_open.py

      - name: Commit and push memory updates
        run: |
          git config user.name "rocky-bot"
          git config user.email "rocky-bot@users.noreply.github.com"
          git add memory/ || true
          if ! git diff --staged --quiet; then
            DATE=$(date -u +"%Y-%m-%d")
            git commit -m "market-open: $DATE | trade execution"
            git pull --rebase origin master || true
            git push origin HEAD:master
          else
            echo "No memory changes to commit."
          fi
